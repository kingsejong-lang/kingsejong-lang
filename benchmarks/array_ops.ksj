# 배열 조작 벤치마크

# 큰 배열 생성
배열 = []
i가 1부터 1000까지 {
    배열 = 배열에_추가(배열, i)
}

# 배열 접근
합계 = 0
i가 0부터 길이(배열) - 1까지 {
    합계 = 합계 + 배열[i]
}

# 배열 정렬 (간단한 버블 정렬)
정렬된배열 = []
i가 100부터 1까지 {
    정렬된배열 = 배열에_추가(정렬된배열, i)
}

# 간단한 정렬 (선택 정렬)
n = 길이(정렬된배열)
i가 0부터 n - 2까지 {
    최소_인덱스 = i
    j가 i + 1부터 n - 1까지 {
        만약 (정렬된배열[j] < 정렬된배열[최소_인덱스]) {
            최소_인덱스 = j
        }
    }
    # 교환
    임시 = 정렬된배열[i]
    정렬된배열[i] = 정렬된배열[최소_인덱스]
    정렬된배열[최소_인덱스] = 임시
}

출력("배열 조작 완료: 합계=" + 문자열로(합계))
