# 메모리 사용량 벤치마크
# 다양한 데이터 구조와 연산의 메모리 사용량 테스트

출력("메모리 테스트 시작")

# 1. 중첩 배열
중첩배열 = [[1, 2, 3], [4, 5, 6], [7, 8, 9]]

# 2. 문자열 연산
긴문자열 = ""
j가 0부터 99까지 {
    긴문자열 = 긴문자열 + "안녕"
}

# 3. 계산 집약적 작업
합계 = 0
m가 0부터 9999까지 {
    합계 = 합계 + m
}

# 4. 중첩 반복문
곱셈합 = 0
i가 1부터 100까지 {
    j가 1부터 100까지 {
        곱셈합 = 곱셈합 + (i * j)
    }
}

출력("메모리 테스트 완료")
출력(길이(중첩배열))
출력(합계)
출력(곱셈합)
