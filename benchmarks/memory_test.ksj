// 메모리 사용량 벤치마크
// 다양한 데이터 구조와 연산의 메모리 사용량 테스트

// 1. 큰 배열 생성
정수 배열크기 = 1000
배열 큰배열 = []
정수 i = 0
반복 (i < 배열크기) {
    큰배열.추가(i * 2)
    i = i + 1
}

// 2. 문자열 연산
문자열 긴문자열 = ""
정수 j = 0
반복 (j < 100) {
    긴문자열 = 긴문자열 + "안녕하세요"
    j = j + 1
}

// 3. 중첩 배열
배열 중첩배열 = []
정수 k = 0
반복 (k < 100) {
    배열 내부배열 = [k, k * 2, k * 3]
    중첩배열.추가(내부배열)
    k = k + 1
}

// 4. 계산 집약적 작업
정수 합계 = 0
정수 m = 0
반복 (m < 10000) {
    합계 = 합계 + m
    m = m + 1
}

출력("메모리 테스트 완료")
출력(큰배열.길이())
출력(중첩배열.길이())
출력(합계)
