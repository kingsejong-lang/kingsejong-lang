# 메모리 사용량 벤치마크
# 다양한 데이터 구조와 연산의 메모리 사용량 테스트

# 1. 배열 생성
큰배열 = []
i가 0부터 99까지 {
    큰배열 = 배열에_추가(큰배열, i * 2)
}

# 2. 문자열 연산
긴문자열 = ""
j가 0부터 49까지 {
    긴문자열 = 긴문자열 + "안녕"
}

# 3. 중첩 배열
중첩배열 = []
k가 0부터 49까지 {
    내부배열 = [k, k * 2, k * 3]
    중첩배열 = 배열에_추가(중첩배열, 내부배열)
}

# 4. 계산 집약적 작업
합계 = 0
m가 0부터 999까지 {
    합계 = 합계 + m
}

출력("메모리 테스트 완료")
출력(길이(큰배열))
출력(길이(중첩배열))
출력(합계)
