# KingSejong 표준 라이브러리 레퍼런스

> **버전**: v0.5.0
> **총 함수 수**: 251개
> **최종 업데이트**: 2025-11-21

---

## 목차

1. [array.ksj](#1-arrayksj) - 배열 유틸리티 (15개)
2. [collections.ksj](#2-collectionsksj) - 자료구조 (49개)
3. [crypto.ksj](#3-cryptoksj) - 암호화 (35개)
4. [db.ksj](#4-dbksj) - 데이터베이스 (14개)
5. [http.ksj](#5-httpksj) - HTTP 클라이언트 (21개)
6. [io.ksj](#6-ioksj) - 파일 입출력 (14개)
7. [json.ksj](#7-jsonksj) - JSON 처리 (13개)
8. [math.ksj](#8-mathksj) - 수학 함수 (14개)
9. [os.ksj](#9-osksj) - 운영체제 (15개)
10. [regex.ksj](#10-regexksj) - 정규표현식 (21개)
11. [string.ksj](#11-stringksj) - 문자열 (10개)
12. [test.ksj](#12-testksj) - 테스트 프레임워크 (11개)
13. [time.ksj](#13-timeksj) - 시간/날짜 (7개)
14. [utils.ksj](#14-utilsksj) - 유틸리티 (23개)

---

## 1. array.ksj

배열 조작 및 고차 함수

### 주요 함수

| 함수 | 설명 | 예제 |
|------|------|------|
| `배열_맵(arr, fn)` | 각 요소에 함수 적용 | `배열_맵([1,2,3], 함수(x) { 반환 x*2 })` → `[2,4,6]` |
| `배열_필터(arr, fn)` | 조건 만족 요소만 선택 | `배열_필터([1,2,3,4], 함수(x) { 반환 x > 2 })` → `[3,4]` |
| `배열_리듀스(arr, fn, init)` | 누적 계산 | `배열_리듀스([1,2,3], 함수(a,b) { 반환 a+b }, 0)` → `6` |
| `배열_정렬(arr)` | 오름차순 정렬 | `배열_정렬([3,1,2])` → `[1,2,3]` |
| `배열_역순(arr)` | 배열 역순 정렬 | `배열_역순([1,2,3])` → `[3,2,1]` |
| `배열_합(arr)` | 모든 요소의 합 | `배열_합([1,2,3])` → `6` |
| `배열_평균(arr)` | 평균 값 | `배열_평균([1,2,3])` → `2` |
| `배열_최대(arr)` | 최댓값 | `배열_최대([1,5,3])` → `5` |
| `배열_최소(arr)` | 최솟값 | `배열_최소([1,5,3])` → `1` |
| `배열_포함(arr, val)` | 값 포함 여부 | `배열_포함([1,2,3], 2)` → `참` |

### 사용 예제

```ksj
# 배열 고차 함수 예제
숫자들 = [1, 2, 3, 4, 5]

# 맵: 각 요소를 2배로
두배 = 배열_맵(숫자들, 함수(x) { 반환 x * 2 })
출력(두배)  # [2, 4, 6, 8, 10]

# 필터: 짝수만 선택
짝수 = 배열_필터(숫자들, 함수(x) { 반환 x % 2 == 0 })
출력(짝수)  # [2, 4]

# 리듀스: 모든 요소의 곱
곱 = 배열_리듀스(숫자들, 함수(acc, x) { 반환 acc * x }, 1)
출력(곱)  # 120
```

---

## 2. collections.ksj

고급 자료구조: Set, Map, Queue, Stack, Deque

### Set (집합) - 9개 함수

| 함수 | 설명 | 예제 |
|------|------|------|
| `Set_생성()` | 빈 Set 생성 | `집합 = Set_생성()` |
| `Set_추가(set, val)` | 값 추가 (중복 무시) | `Set_추가(집합, 5)` |
| `Set_제거(set, val)` | 값 제거 | `Set_제거(집합, 5)` |
| `Set_포함(set, val)` | 포함 여부 | `Set_포함(집합, 5)` → `참/거짓` |
| `Set_크기(set)` | 크기 반환 | `Set_크기(집합)` → `3` |
| `Set_합집합(s1, s2)` | 합집합 (A ∪ B) | `Set_합집합(집합1, 집합2)` |
| `Set_교집합(s1, s2)` | 교집합 (A ∩ B) | `Set_교집합(집합1, 집합2)` |
| `Set_차집합(s1, s2)` | 차집합 (A - B) | `Set_차집합(집합1, 집합2)` |
| `Set_대칭차집합(s1, s2)` | 대칭차집합 (A Δ B) | `Set_대칭차집합(집합1, 집합2)` |

### Map (딕셔너리) - 8개 함수

| 함수 | 설명 | 예제 |
|------|------|------|
| `Map_생성()` | 빈 Map 생성 | `맵 = Map_생성()` |
| `Map_설정(map, key, val)` | 키-값 설정 | `Map_설정(맵, "이름", "철수")` |
| `Map_가져오기(map, key)` | 값 가져오기 | `Map_가져오기(맵, "이름")` → `"철수"` |
| `Map_포함(map, key)` | 키 존재 여부 | `Map_포함(맵, "이름")` → `참/거짓` |
| `Map_제거(map, key)` | 키 제거 | `Map_제거(맵, "이름")` |
| `Map_크기(map)` | 크기 반환 | `Map_크기(맵)` → `5` |
| `Map_키목록(map)` | 모든 키 반환 | `Map_키목록(맵)` → `["이름", "나이"]` |
| `Map_값목록(map)` | 모든 값 반환 | `Map_값목록(맵)` → `["철수", 25]` |

### Queue (큐) - 8개 함수

| 함수 | 설명 | 예제 |
|------|------|------|
| `Queue_생성()` | 빈 Queue 생성 | `큐 = Queue_생성()` |
| `Queue_삽입(q, val)` | 뒤에 추가 (enqueue) | `Queue_삽입(큐, 10)` |
| `Queue_제거(q)` | 앞에서 제거 (dequeue) | `Queue_제거(큐)` → `10` |
| `Queue_앞(q)` | 맨 앞 요소 확인 | `Queue_앞(큐)` → `10` |
| `Queue_크기(q)` | 크기 반환 | `Queue_크기(큐)` → `3` |
| `Queue_비어있는가(q)` | 비어있는지 확인 | `Queue_비어있는가(큐)` → `참/거짓` |
| `Queue_초기화(q)` | 큐 비우기 | `Queue_초기화(큐)` |
| `Queue_배열로(q)` | 배열로 변환 | `Queue_배열로(큐)` → `[1,2,3]` |

### Stack (스택) - 8개 함수

| 함수 | 설명 | 예제 |
|------|------|------|
| `Stack_생성()` | 빈 Stack 생성 | `스택 = Stack_생성()` |
| `Stack_삽입(s, val)` | 위에 추가 (push) | `Stack_삽입(스택, 10)` |
| `Stack_제거(s)` | 위에서 제거 (pop) | `Stack_제거(스택)` → `10` |
| `Stack_위(s)` | 맨 위 요소 확인 (peek) | `Stack_위(스택)` → `10` |
| `Stack_크기(s)` | 크기 반환 | `Stack_크기(스택)` → `3` |
| `Stack_비어있는가(s)` | 비어있는지 확인 | `Stack_비어있는가(스택)` → `참/거짓` |
| `Stack_초기화(s)` | 스택 비우기 | `Stack_초기화(스택)` |
| `Stack_배열로(s)` | 배열로 변환 | `Stack_배열로(스택)` → `[1,2,3]` |

### Deque (양방향 큐) - 8개 함수

| 함수 | 설명 | 예제 |
|------|------|------|
| `Deque_생성()` | 빈 Deque 생성 | `덱 = Deque_생성()` |
| `Deque_앞삽입(d, val)` | 앞에 추가 | `Deque_앞삽입(덱, 10)` |
| `Deque_뒤삽입(d, val)` | 뒤에 추가 | `Deque_뒤삽입(덱, 20)` |
| `Deque_앞제거(d)` | 앞에서 제거 | `Deque_앞제거(덱)` → `10` |
| `Deque_뒤제거(d)` | 뒤에서 제거 | `Deque_뒤제거(덱)` → `20` |
| `Deque_앞(d)` | 맨 앞 요소 확인 | `Deque_앞(덱)` → `10` |
| `Deque_뒤(d)` | 맨 뒤 요소 확인 | `Deque_뒤(덱)` → `20` |
| `Deque_크기(d)` | 크기 반환 | `Deque_크기(덱)` → `5` |

### 사용 예제

```ksj
# Set 예제: 중복 제거
집합 = Set_생성()
집합 = Set_추가(집합, 1)
집합 = Set_추가(집합, 2)
집합 = Set_추가(집합, 1)  # 중복 무시
출력(집합)  # [1, 2]

# Queue 예제: FIFO
큐 = Queue_생성()
큐 = Queue_삽입(큐, "첫번째")
큐 = Queue_삽입(큐, "두번째")
출력(Queue_제거(큐))  # "첫번째"
출력(Queue_제거(큐))  # "두번째"

# Stack 예제: LIFO
스택 = Stack_생성()
스택 = Stack_삽입(스택, "첫번째")
스택 = Stack_삽입(스택, "두번째")
출력(Stack_제거(스택))  # "두번째"
출력(Stack_제거(스택))  # "첫번째"
```

---

## 3. crypto.ksj

암호화 및 해싱 함수 (35개)

### 해시 함수 (11개)

| 함수 | 설명 | 예제 |
|------|------|------|
| `MD5(text)` | MD5 해시 | `MD5("hello")` → `"5d41402abc4b2a76b9719d911017c592"` |
| `SHA1(text)` | SHA-1 해시 | `SHA1("hello")` |
| `SHA256(text)` | SHA-256 해시 | `SHA256("hello")` |
| `SHA512(text)` | SHA-512 해시 | `SHA512("hello")` |
| `HMAC_SHA256(key, msg)` | HMAC-SHA256 | `HMAC_SHA256("key", "message")` |

### 대칭키 암호화 (10개)

| 함수 | 설명 | 예제 |
|------|------|------|
| `AES_암호화(plain, key)` | AES 암호화 | `AES_암호화("비밀", "키")` |
| `AES_복호화(cipher, key)` | AES 복호화 | `AES_복호화(암호문, "키")` |
| `ChaCha20_암호화(plain, key)` | ChaCha20 암호화 | `ChaCha20_암호화("비밀", "키")` |
| `ChaCha20_복호화(cipher, key)` | ChaCha20 복호화 | `ChaCha20_복호화(암호문, "키")` |

### 비대칭키 암호화 (6개)

| 함수 | 설명 | 예제 |
|------|------|------|
| `RSA_키쌍생성(bits)` | RSA 키쌍 생성 | `RSA_키쌍생성(2048)` |
| `RSA_암호화(plain, pubkey)` | RSA 공개키 암호화 | `RSA_암호화("비밀", 공개키)` |
| `RSA_복호화(cipher, prikey)` | RSA 개인키 복호화 | `RSA_복호화(암호문, 개인키)` |
| `RSA_서명(msg, prikey)` | RSA 서명 생성 | `RSA_서명("메시지", 개인키)` |
| `RSA_검증(msg, sig, pubkey)` | RSA 서명 검증 | `RSA_검증("메시지", 서명, 공개키)` |

### 인코딩 (8개)

| 함수 | 설명 | 예제 |
|------|------|------|
| `Base64_인코딩(data)` | Base64 인코딩 | `Base64_인코딩("hello")` → `"aGVsbG8="` |
| `Base64_디코딩(encoded)` | Base64 디코딩 | `Base64_디코딩("aGVsbG8=")` → `"hello"` |
| `Hex_인코딩(data)` | 16진수 인코딩 | `Hex_인코딩("hello")` → `"68656c6c6f"` |
| `Hex_디코딩(hex)` | 16진수 디코딩 | `Hex_디코딩("68656c6c6f")` → `"hello"` |

### 사용 예제

```ksj
# SHA-256 해시
비밀번호 = "my_password"
해시 = SHA256(비밀번호)
출력("SHA-256:", 해시)

# AES 암호화/복호화
평문 = "중요한 정보"
키 = "비밀키1234567890"
암호문 = AES_암호화(평문, 키)
복호화 = AES_복호화(암호문, 키)
출력("원본:", 복호화)  # "중요한 정보"

# Base64 인코딩
데이터 = "Hello, World!"
인코딩 = Base64_인코딩(데이터)
출력("Base64:", 인코딩)  # "SGVsbG8sIFdvcmxkIQ=="
```

---

## 4. db.ksj

데이터베이스 연동 함수 (14개)

### 연결 관리 (4개)

| 함수 | 설명 | 예제 |
|------|------|------|
| `DB_연결(type, config)` | DB 연결 | `DB_연결("sqlite", {"path": "test.db"})` |
| `DB_연결종료(conn)` | 연결 종료 | `DB_연결종료(연결)` |
| `DB_연결확인(conn)` | 연결 상태 확인 | `DB_연결확인(연결)` → `참/거짓` |
| `DB_재연결(conn)` | 재연결 | `DB_재연결(연결)` |

### 쿼리 실행 (6개)

| 함수 | 설명 | 예제 |
|------|------|------|
| `DB_쿼리(conn, sql)` | SQL 쿼리 실행 | `DB_쿼리(연결, "SELECT * FROM users")` |
| `DB_삽입(conn, table, data)` | 데이터 삽입 | `DB_삽입(연결, "users", {"name": "철수"})` |
| `DB_업데이트(conn, table, data, where)` | 데이터 업데이트 | `DB_업데이트(연결, "users", {"age": 26}, "name='철수'")` |
| `DB_삭제(conn, table, where)` | 데이터 삭제 | `DB_삭제(연결, "users", "id=5")` |
| `DB_선택(conn, table, where)` | 데이터 조회 | `DB_선택(연결, "users", "age > 20")` |
| `DB_카운트(conn, table, where)` | 개수 세기 | `DB_카운트(연결, "users", "age > 20")` → `15` |

### 트랜잭션 (4개)

| 함수 | 설명 | 예제 |
|------|------|------|
| `DB_트랜잭션시작(conn)` | 트랜잭션 시작 | `DB_트랜잭션시작(연결)` |
| `DB_커밋(conn)` | 변경사항 저장 | `DB_커밋(연결)` |
| `DB_롤백(conn)` | 변경사항 취소 | `DB_롤백(연결)` |
| `DB_트랜잭션종료(conn)` | 트랜잭션 종료 | `DB_트랜잭션종료(연결)` |

### 사용 예제

```ksj
# SQLite 데이터베이스 사용
연결 = DB_연결("sqlite", {"path": "users.db"})

# 사용자 추가
DB_삽입(연결, "users", {
    "name": "김철수",
    "age": 25,
    "email": "chulsoo@example.com"
})

# 사용자 조회
결과 = DB_선택(연결, "users", "age >= 20")
출력("성인 사용자:", 결과)

# 트랜잭션 사용
DB_트랜잭션시작(연결)
DB_업데이트(연결, "users", {"age": 26}, "name='김철수'")
DB_커밋(연결)

DB_연결종료(연결)
```

---

## 5. http.ksj

HTTP 클라이언트 함수 (21개)

### HTTP 요청 (8개)

| 함수 | 설명 | 예제 |
|------|------|------|
| `HTTP_GET(url, headers)` | GET 요청 | `HTTP_GET("https://api.example.com/users")` |
| `HTTP_POST(url, data, headers)` | POST 요청 | `HTTP_POST(url, {"name": "철수"}, headers)` |
| `HTTP_PUT(url, data, headers)` | PUT 요청 | `HTTP_PUT(url, data, headers)` |
| `HTTP_DELETE(url, headers)` | DELETE 요청 | `HTTP_DELETE(url, headers)` |
| `HTTP_PATCH(url, data, headers)` | PATCH 요청 | `HTTP_PATCH(url, data, headers)` |
| `HTTP_HEAD(url, headers)` | HEAD 요청 | `HTTP_HEAD(url, headers)` |
| `HTTP_OPTIONS(url, headers)` | OPTIONS 요청 | `HTTP_OPTIONS(url, headers)` |
| `HTTP_요청(method, url, data, headers)` | 커스텀 요청 | `HTTP_요청("GET", url, null, headers)` |

### 응답 처리 (5개)

| 함수 | 설명 | 예제 |
|------|------|------|
| `HTTP_응답파싱(response)` | 응답 파싱 | `HTTP_응답파싱(응답)` |
| `HTTP_상태코드(response)` | 상태 코드 추출 | `HTTP_상태코드(응답)` → `200` |
| `HTTP_헤더(response, key)` | 헤더 값 추출 | `HTTP_헤더(응답, "Content-Type")` |
| `HTTP_바디(response)` | 응답 본문 추출 | `HTTP_바디(응답)` |
| `HTTP_JSON파싱(response)` | JSON 응답 파싱 | `HTTP_JSON파싱(응답)` |

### 헤더 & URL (4개)

| 함수 | 설명 | 예제 |
|------|------|------|
| `HTTP_헤더생성(headers)` | 헤더 객체 생성 | `HTTP_헤더생성({"Authorization": "Bearer token"})` |
| `HTTP_쿼리스트링(params)` | 쿼리 스트링 생성 | `HTTP_쿼리스트링({"page": 1})` → `"page=1"` |
| `HTTP_URL인코딩(str)` | URL 인코딩 | `HTTP_URL인코딩("안녕 세계")` |
| `HTTP_URL디코딩(str)` | URL 디코딩 | `HTTP_URL디코딩("%EC%95%88%EB%85%95")` |

### 사용 예제

```ksj
# REST API 호출
응답 = HTTP_GET("https://api.github.com/users/octocat")

만약 (HTTP_상태코드(응답) == 200) {
    데이터 = HTTP_JSON파싱(응답)
    출력("사용자:", 데이터["name"])
} 아니면 {
    출력("오류:", HTTP_상태코드(응답))
}

# POST 요청으로 데이터 전송
헤더 = HTTP_헤더생성({
    "Content-Type": "application/json",
    "Authorization": "Bearer my-token"
})

응답 = HTTP_POST("https://api.example.com/users", {
    "name": "김철수",
    "age": 25
}, 헤더)

출력("응답:", HTTP_바디(응답))
```

---

## 6. io.ksj

파일 입출력 함수 (14개)

### 파일 읽기/쓰기 (6개)

| 함수 | 설명 | 예제 |
|------|------|------|
| `파일_읽기(path)` | 파일 내용 읽기 | `파일_읽기("test.txt")` → 문자열 |
| `파일_쓰기(path, content)` | 파일에 쓰기 (덮어쓰기) | `파일_쓰기("test.txt", "내용")` |
| `파일_추가(path, content)` | 파일에 추가 | `파일_추가("log.txt", "새 줄\n")` |
| `파일_줄읽기(path)` | 줄 단위로 읽기 | `파일_줄읽기("test.txt")` → 배열 |
| `파일_바이트읽기(path)` | 바이트 단위 읽기 | `파일_바이트읽기("image.png")` |
| `파일_바이트쓰기(path, bytes)` | 바이트 단위 쓰기 | `파일_바이트쓰기("image.png", 바이트)` |

### 파일 관리 (4개)

| 함수 | 설명 | 예제 |
|------|------|------|
| `파일_존재(path)` | 파일 존재 여부 | `파일_존재("test.txt")` → `참/거짓` |
| `파일_삭제(path)` | 파일 삭제 | `파일_삭제("temp.txt")` |
| `파일_복사(src, dst)` | 파일 복사 | `파일_복사("a.txt", "b.txt")` |
| `파일_이동(src, dst)` | 파일 이동 | `파일_이동("old.txt", "new.txt")` |

### 디렉토리 (4개)

| 함수 | 설명 | 예제 |
|------|------|------|
| `디렉토리_생성(path)` | 디렉토리 생성 | `디렉토리_생성("temp")` |
| `디렉토리_삭제(path)` | 디렉토리 삭제 | `디렉토리_삭제("temp")` |
| `디렉토리_목록(path)` | 파일/폴더 목록 | `디렉토리_목록(".")` → 배열 |
| `디렉토리_존재(path)` | 디렉토리 존재 여부 | `디렉토리_존재("temp")` → `참/거짓` |

### 사용 예제

```ksj
# 파일 읽기/쓰기
내용 = 파일_읽기("input.txt")
출력("파일 내용:", 내용)

파일_쓰기("output.txt", "새로운 내용")

# 로그 파일에 추가
파일_추가("log.txt", "2025-11-21: 애플리케이션 시작\n")

# 디렉토리 작업
디렉토리_생성("temp")
파일목록 = 디렉토리_목록("temp")
출력("파일 목록:", 파일목록)
```

---

## 7. json.ksj

JSON 처리 함수 (13개)

### JSON 변환 (4개)

| 함수 | 설명 | 예제 |
|------|------|------|
| `JSON_파싱(str)` | JSON 문자열 → 객체 | `JSON_파싱('{"name":"철수"}')` |
| `JSON_문자열화(obj)` | 객체 → JSON 문자열 | `JSON_문자열화({"name": "철수"})` |
| `JSON_예쁘게(obj, indent)` | 들여쓰기 JSON | `JSON_예쁘게(obj, 2)` |
| `JSON_압축(obj)` | 공백 제거 JSON | `JSON_압축(obj)` |

### JSON 검증 (3개)

| 함수 | 설명 | 예제 |
|------|------|------|
| `JSON_유효(str)` | JSON 유효성 검사 | `JSON_유효('{"valid":true}')` → `참` |
| `JSON_타입(val)` | JSON 타입 확인 | `JSON_타입(42)` → `"number"` |
| `JSON_스키마검증(obj, schema)` | 스키마 검증 | `JSON_스키마검증(obj, schema)` |

### JSON 조작 (6개)

| 함수 | 설명 | 예제 |
|------|------|------|
| `JSON_가져오기(obj, path)` | 경로로 값 가져오기 | `JSON_가져오기(obj, "user.name")` |
| `JSON_설정(obj, path, val)` | 경로로 값 설정 | `JSON_설정(obj, "user.age", 26)` |
| `JSON_삭제(obj, path)` | 경로로 값 삭제 | `JSON_삭제(obj, "user.email")` |
| `JSON_병합(obj1, obj2)` | 두 객체 병합 | `JSON_병합(obj1, obj2)` |
| `JSON_복사(obj)` | 깊은 복사 | `JSON_복사(obj)` |
| `JSON_비교(obj1, obj2)` | 객체 비교 | `JSON_비교(obj1, obj2)` → `참/거짓` |

### 사용 예제

```ksj
# JSON 파싱
문자열 = '{"name": "김철수", "age": 25}'
객체 = JSON_파싱(문자열)
출력("이름:", 객체["name"])

# JSON 문자열화
데이터 = {
    "사용자": "영희",
    "점수": [90, 85, 95]
}
json문자열 = JSON_예쁘게(데이터, 2)
출력(json문자열)

# 경로로 값 접근
사용자 = {
    "profile": {
        "name": "철수",
        "contact": {"email": "chulsoo@example.com"}
    }
}
이메일 = JSON_가져오기(사용자, "profile.contact.email")
출력("이메일:", 이메일)
```

---

## 8. math.ksj

수학 함수 (14개)

### 기본 함수 (8개)

| 함수 | 설명 | 예제 |
|------|------|------|
| `절댓값(x)` | 절댓값 | `절댓값(-5)` → `5` |
| `올림(x)` | 올림 | `올림(3.2)` → `4` |
| `내림(x)` | 내림 | `내림(3.8)` → `3` |
| `반올림(x)` | 반올림 | `반올림(3.5)` → `4` |
| `제곱(x)` | 제곱 | `제곱(3)` → `9` |
| `제곱근(x)` | 제곱근 | `제곱근(9)` → `3` |
| `거듭제곱(x, n)` | x의 n제곱 | `거듭제곱(2, 10)` → `1024` |
| `로그(x)` | 자연로그 | `로그(10)` → `2.302...` |

### 삼각함수 (6개)

| 함수 | 설명 | 예제 |
|------|------|------|
| `사인(x)` | 사인 (라디안) | `사인(1.57)` → `1.0` |
| `코사인(x)` | 코사인 | `코사인(0)` → `1.0` |
| `탄젠트(x)` | 탄젠트 | `탄젠트(0.785)` → `1.0` |
| `아크사인(x)` | 아크사인 | `아크사인(1)` → `1.57` |
| `아크코사인(x)` | 아크코사인 | `아크코사인(0)` → `1.57` |
| `아크탄젠트(x)` | 아크탄젠트 | `아크탄젠트(1)` → `0.785` |

### 사용 예제

```ksj
# 원의 면적 계산
반지름 = 5
파이 = 3.14159
면적 = 파이 * 제곱(반지름)
출력("면적:", 면적)  # 78.5

# 피타고라스 정리
a = 3
b = 4
c = 제곱근(제곱(a) + 제곱(b))
출력("빗변:", c)  # 5
```

---

## 9. os.ksj

운영체제 함수 (15개)

### 환경 변수 (4개)

| 함수 | 설명 | 예제 |
|------|------|------|
| `환경변수_가져오기(name)` | 환경 변수 조회 | `환경변수_가져오기("PATH")` |
| `환경변수_설정(name, val)` | 환경 변수 설정 | `환경변수_설정("DEBUG", "true")` |
| `환경변수_삭제(name)` | 환경 변수 삭제 | `환경변수_삭제("TEMP")` |
| `환경변수_목록()` | 모든 환경 변수 | `환경변수_목록()` → 객체 |

### 프로세스 (5개)

| 함수 | 설명 | 예제 |
|------|------|------|
| `프로세스_실행(cmd, args)` | 프로세스 실행 | `프로세스_실행("ls", ["-la"])` |
| `프로세스_ID()` | 현재 PID | `프로세스_ID()` → `12345` |
| `프로세스_종료(code)` | 프로세스 종료 | `프로세스_종료(0)` |
| `프로세스_대기(pid)` | 프로세스 대기 | `프로세스_대기(12345)` |
| `프로세스_신호(pid, sig)` | 신호 전송 | `프로세스_신호(12345, "TERM")` |

### 시스템 정보 (6개)

| 함수 | 설명 | 예제 |
|------|------|------|
| `OS_이름()` | OS 이름 | `OS_이름()` → `"Linux"` |
| `OS_버전()` | OS 버전 | `OS_버전()` → `"5.10.0"` |
| `OS_아키텍처()` | CPU 아키텍처 | `OS_아키텍처()` → `"x86_64"` |
| `OS_CPU개수()` | CPU 코어 수 | `OS_CPU개수()` → `8` |
| `OS_메모리()` | 총 메모리 (MB) | `OS_메모리()` → `16384` |
| `OS_디스크용량(path)` | 디스크 용량 | `OS_디스크용량("/")` |

### 사용 예제

```ksj
# 시스템 정보 출력
출력("OS:", OS_이름())
출력("아키텍처:", OS_아키텍처())
출력("CPU 코어:", OS_CPU개수())
출력("메모리:", OS_메모리(), "MB")

# 환경 변수 사용
홈디렉토리 = 환경변수_가져오기("HOME")
출력("홈:", 홈디렉토리)

# 외부 명령 실행
결과 = 프로세스_실행("echo", ["Hello, World!"])
출력("결과:", 결과)
```

---

## 10. regex.ksj

정규표현식 함수 (21개)

### 패턴 매칭 (6개)

| 함수 | 설명 | 예제 |
|------|------|------|
| `정규식_매치(pattern, text)` | 패턴 매칭 | `정규식_매치("^hello", "hello world")` → `참` |
| `정규식_찾기(pattern, text)` | 첫 매치 찾기 | `정규식_찾기("\\d+", "abc123def")` → `"123"` |
| `정규식_모두찾기(pattern, text)` | 모든 매치 찾기 | `정규식_모두찾기("\\d+", "a1b2c3")` → `["1","2","3"]` |
| `정규식_대체(pattern, repl, text)` | 패턴 대체 | `정규식_대체("\\d+", "X", "a1b2")` → `"aXbX"` |
| `정규식_분리(pattern, text)` | 패턴으로 분리 | `정규식_분리(",\\s*", "a, b, c")` → `["a","b","c"]` |
| `정규식_검증(pattern)` | 패턴 유효성 검사 | `정규식_검증("[a-z]+")` → `참` |

### 사전 정의 패턴 (15개)

| 함수 | 설명 | 예제 |
|------|------|------|
| `이메일_검증(email)` | 이메일 유효성 | `이메일_검증("test@example.com")` → `참` |
| `URL_검증(url)` | URL 유효성 | `URL_검증("https://example.com")` → `참` |
| `전화번호_검증(phone)` | 전화번호 유효성 (한국) | `전화번호_검증("010-1234-5678")` → `참` |
| `IP주소_검증(ip)` | IPv4 유효성 | `IP주소_검증("192.168.1.1")` → `참` |
| `날짜_검증(date)` | 날짜 유효성 (YYYY-MM-DD) | `날짜_검증("2025-11-21")` → `참` |
| `숫자만_추출(text)` | 숫자만 추출 | `숫자만_추출("abc123def456")` → `"123456"` |
| `문자만_추출(text)` | 문자만 추출 | `문자만_추출("abc123def")` → `"abcdef"` |
| `공백_제거(text)` | 모든 공백 제거 | `공백_제거("a b c")` → `"abc"` |
| `HTML태그_제거(html)` | HTML 태그 제거 | `HTML태그_제거("<p>text</p>")` → `"text"` |

### 사용 예제

```ksj
# 이메일 검증
이메일 = "user@example.com"
만약 (이메일_검증(이메일)) {
    출력("유효한 이메일입니다")
}

# 숫자 추출
텍스트 = "가격은 10,000원입니다"
가격 = 숫자만_추출(텍스트)
출력("가격:", 가격)  # "10000"

# 패턴 매칭
전화번호들 = "010-1234-5678, 02-123-4567"
번호들 = 정규식_모두찾기("\\d{2,3}-\\d{3,4}-\\d{4}", 전화번호들)
출력("번호 목록:", 번호들)
```

---

## 11. string.ksj

문자열 함수 (10개)

| 함수 | 설명 | 예제 |
|------|------|------|
| `문자열_대문자(str)` | 대문자 변환 | `문자열_대문자("hello")` → `"HELLO"` |
| `문자열_소문자(str)` | 소문자 변환 | `문자열_소문자("HELLO")` → `"hello"` |
| `문자열_자르기(str, start, end)` | 부분 문자열 | `문자열_자르기("hello", 1, 4)` → `"ell"` |
| `문자열_분리(str, sep)` | 구분자로 분리 | `문자열_분리("a,b,c", ",")` → `["a","b","c"]` |
| `문자열_결합(arr, sep)` | 배열을 문자열로 결합 | `문자열_결합(["a","b"], "-")` → `"a-b"` |
| `문자열_대체(str, old, new)` | 문자열 대체 | `문자열_대체("hello", "l", "L")` → `"heLLo"` |
| `문자열_시작(str, prefix)` | 접두사 확인 | `문자열_시작("hello", "hel")` → `참` |
| `문자열_끝(str, suffix)` | 접미사 확인 | `문자열_끝("hello", "lo")` → `참` |
| `문자열_반복(str, n)` | 문자열 반복 | `문자열_반복("ab", 3)` → `"ababab"` |
| `문자열_역순(str)` | 문자열 역순 | `문자열_역순("hello")` → `"olleh"` |

### 사용 예제

```ksj
# 문자열 조작
이름 = "kim chulsoo"
대문자 = 문자열_대문자(이름)
출력(대문자)  # "KIM CHULSOO"

# CSV 파싱
csv = "사과,바나나,오렌지"
과일들 = 문자열_분리(csv, ",")
출력(과일들)  # ["사과", "바나나", "오렌지"]

# 경로 조작
파일명 = "document.pdf"
만약 (문자열_끝(파일명, ".pdf")) {
    출력("PDF 파일입니다")
}
```

---

## 12. test.ksj

테스트 프레임워크 (11개)

### 테스트 관리 (4개)

| 함수 | 설명 | 예제 |
|------|------|------|
| `테스트_시작(name)` | 테스트 스위트 시작 | `테스트_시작("배열 테스트")` |
| `테스트_종료()` | 테스트 스위트 종료 | `테스트_종료()` |
| `테스트_실행(name, fn)` | 테스트 케이스 실행 | `테스트_실행("덧셈", 함수() {...})` |
| `테스트_결과()` | 결과 반환 | `테스트_결과()` → 객체 |

### 단언문 (7개)

| 함수 | 설명 | 예제 |
|------|------|------|
| `단언_같음(actual, expected)` | 값 비교 | `단언_같음(2+2, 4)` |
| `단언_참(value)` | 참 확인 | `단언_참(길이([1,2]) == 2)` |
| `단언_거짓(value)` | 거짓 확인 | `단언_거짓(배열_비어있는가([1]))` |
| `단언_Null(value)` | Null 확인 | `단언_Null(변수)` |
| `단언_타입(value, type)` | 타입 확인 | `단언_타입(42, "정수")` |
| `단언_포함(arr, item)` | 포함 확인 | `단언_포함([1,2,3], 2)` |
| `단언_예외(fn)` | 예외 발생 확인 | `단언_예외(함수() { 오류발생 })` |

### 사용 예제

```ksj
# 테스트 스위트 작성
테스트_시작("수학 함수 테스트")

테스트_실행("덧셈 테스트", 함수() {
    단언_같음(2 + 2, 4)
    단언_같음(10 + 5, 15)
})

테스트_실행("배열 테스트", 함수() {
    arr = [1, 2, 3]
    단언_포함(arr, 2)
    단언_같음(길이(arr), 3)
})

결과 = 테스트_종료()
출력("통과:", 결과["통과"])
출력("실패:", 결과["실패"])
```

---

## 13. time.ksj

시간/날짜 함수 (7개)

| 함수 | 설명 | 예제 |
|------|------|------|
| `현재시간()` | 현재 타임스탬프 (초) | `현재시간()` → `1700000000` |
| `현재시간_밀리초()` | 타임스탬프 (밀리초) | `현재시간_밀리초()` → `1700000000000` |
| `날짜_포맷(timestamp, format)` | 날짜 포맷 | `날짜_포맷(현재시간(), "YYYY-MM-DD")` |
| `날짜_파싱(datestr, format)` | 날짜 파싱 | `날짜_파싱("2025-11-21", "YYYY-MM-DD")` |
| `날짜_더하기(timestamp, days)` | 날짜에 일수 추가 | `날짜_더하기(현재시간(), 7)` |
| `날짜_차이(ts1, ts2)` | 날짜 차이 (일) | `날짜_차이(시간1, 시간2)` → `7` |
| `수면(seconds)` | 대기 (초) | `수면(2)` # 2초 대기 |

### 사용 예제

```ksj
# 현재 시간 출력
지금 = 현재시간()
날짜문자열 = 날짜_포맷(지금, "YYYY-MM-DD HH:mm:ss")
출력("현재 시간:", 날짜문자열)

# 7일 후 날짜 계산
일주일후 = 날짜_더하기(지금, 7)
출력("7일 후:", 날짜_포맷(일주일후, "YYYY-MM-DD"))

# 타이머
출력("3초 대기 중...")
수면(3)
출력("완료!")
```

---

## 14. utils.ksj

유틸리티 함수 (23개)

### 타입 체크 (7개)

| 함수 | 설명 | 예제 |
|------|------|------|
| `타입(value)` | 타입 반환 | `타입(42)` → `"정수"` |
| `정수인가(value)` | 정수 확인 | `정수인가(42)` → `참` |
| `실수인가(value)` | 실수 확인 | `실수인가(3.14)` → `참` |
| `문자열인가(value)` | 문자열 확인 | `문자열인가("hello")` → `참` |
| `배열인가(value)` | 배열 확인 | `배열인가([1,2])` → `참` |
| `함수인가(value)` | 함수 확인 | `함수인가(함수() {})` → `참` |
| `Null인가(value)` | Null 확인 | `Null인가(null)` → `참` |

### 범위 & 반복 (4개)

| 함수 | 설명 | 예제 |
|------|------|------|
| `범위(start, end, step)` | 범위 배열 생성 | `범위(1, 5, 1)` → `[1,2,3,4,5]` |
| `반복(n, fn)` | n번 반복 | `반복(3, 함수() { 출력("Hi") })` |
| `각각(arr, fn)` | 각 요소에 함수 적용 | `각각([1,2,3], 함수(x) { 출력(x) })` |
| `조건반복(condition, fn)` | 조건이 참인 동안 반복 | `조건반복(함수() { 반환 i < 10 }, ...)` |

### 디버깅 (4개)

| 함수 | 설명 | 예제 |
|------|------|------|
| `로그(msgs...)` | 로그 출력 | `로그("디버그:", 변수)` |
| `경고(msg)` | 경고 출력 | `경고("주의!")` |
| `오류(msg)` | 오류 출력 후 종료 | `오류("치명적 오류")` |
| `디버그_출력(obj)` | 객체 구조 출력 | `디버그_출력(복잡한객체)` |

### 유틸 (8개)

| 함수 | 설명 | 예제 |
|------|------|------|
| `무작위(min, max)` | 난수 생성 | `무작위(1, 100)` → `42` |
| `무작위_선택(arr)` | 배열에서 무작위 선택 | `무작위_선택([1,2,3])` → `2` |
| `섞기(arr)` | 배열 섞기 | `섞기([1,2,3])` → `[3,1,2]` |
| `지연실행(fn, ms)` | 함수 지연 실행 | `지연실행(함수() {...}, 1000)` |
| `메모이제이션(fn)` | 함수 결과 캐싱 | `메모이제이션(fibonacci)` |
| `깊은복사(obj)` | 깊은 복사 | `깊은복사(복잡한객체)` |
| `얕은복사(obj)` | 얕은 복사 | `얕은복사(객체)` |
| `객체_병합(obj1, obj2)` | 객체 병합 | `객체_병합(a, b)` |

### 사용 예제

```ksj
# 타입 체크
값 = 42
만약 (정수인가(값)) {
    출력("정수입니다:", 값)
}

# 범위 생성
숫자들 = 범위(1, 10, 2)  # [1, 3, 5, 7, 9]
출력("홀수:", 숫자들)

# 난수
주사위 = 무작위(1, 6)
출력("주사위:", 주사위)

# 메모이제이션 (피보나치 최적화)
피보나치 = 메모이제이션(함수(n) {
    만약 (n <= 1) { 반환 n }
    반환 피보나치(n-1) + 피보나치(n-2)
})

출력("F(100):", 피보나치(100))  # 빠른 계산!
```

---

## 빠른 참조

### 가장 많이 사용되는 함수

1. **배열 조작**: `배열_맵`, `배열_필터`, `배열_리듀스`
2. **HTTP 요청**: `HTTP_GET`, `HTTP_POST`, `HTTP_JSON파싱`
3. **JSON 처리**: `JSON_파싱`, `JSON_문자열화`, `JSON_가져오기`
4. **파일 작업**: `파일_읽기`, `파일_쓰기`, `파일_존재`
5. **문자열**: `문자열_분리`, `문자열_결합`, `문자열_대체`
6. **테스트**: `테스트_실행`, `단언_같음`, `단언_참`
7. **유틸**: `타입`, `범위`, `무작위`, `로그`

---

**문서 업데이트**: 2025-11-21
**총 251개 함수** 모두 정리 완료
**피드백**: https://github.com/anthropics/kingsejong-lang/issues
