정수 합 = 함수(a, b) {
    반환 a + b
}

정수 곱 = 함수(a, b) {
    반환 a * b
}

출력("=== 기본 함수 ===")
출력("3 + 5 =", 합(3, 5))
출력("3 * 5 =", 곱(3, 5))

정수 적용 = 함수(f, x, y) {
    반환 f(x, y)
}

출력("")
출력("=== 고차 함수 ===")
출력("적용(합, 10, 20) =", 적용(합, 10, 20))
출력("적용(곱, 10, 20) =", 적용(곱, 10, 20))
