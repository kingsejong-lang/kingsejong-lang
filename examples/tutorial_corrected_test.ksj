출력("=== 튜토리얼 예제 테스트 (수정된 문법) ===\n")

출력("1. 변수 테스트:")
문자열 이름 = "홍길동"
정수 나이 = 25
출력("이름: " + 이름)
출력("나이: " + 타입(나이))

출력("\n2. 계산 테스트:")
정수 가격 = 5000
정수 개수 = 3
정수 총액 = 가격 * 개수
출력("총액: " + 타입(총액) + "원")

출력("\n3. 조건문 테스트:")
정수 점수 = 85
만약 (점수 >= 90) {
    출력("A 학점")
} 아니면 만약 (점수 >= 80) {
    출력("B 학점")
} 아니면 {
    출력("C 학점")
}

출력("\n4. 반복문 테스트:")
5번 반복한다 {
    출력("반복!")
}

출력("\n5. 범위 반복 테스트:")
정수 i = 1
i가 1부터 5까지 {
    출력("i = " + 타입(i))
}

출력("\n6. 함수 테스트:")
정수 더하기 = 함수(a, b) {
    반환 a + b
}
정수 결과 = 더하기(10, 20)
출력("10 + 20 = " + 타입(결과))

출력("\n=== 테스트 완료 ===")
