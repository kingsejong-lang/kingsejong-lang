# 튜토리얼 예제 테스트

출력("=== 튜토리얼 예제 테스트 시작 ===\n")

# 섹션 3: 변수
출력("1. 변수 테스트:")
이름 = "홍길동"
나이 = 25
출력("이름: " + 이름)
출력("나이: " + 타입(나이))

# 섹션 4: 계산
출력("\n2. 계산 테스트:")
가격 = 5000
개수 = 3
총액 = 가격 * 개수
출력("총액: " + 타입(총액) + "원")

# 섹션 5: 조건문
출력("\n3. 조건문 테스트:")
점수 = 85
만약 (점수 >= 90) {
    출력("A 학점")
} 아니면 만약 (점수 >= 80) {
    출력("B 학점")
} 아니면 {
    출력("C 학점")
}

# 섹션 6: 반복문
출력("\n4. 반복문 테스트:")
5번 반복한다 {
    출력("반복!")
}

출력("\n5. 범위 반복 테스트:")
i가 1부터 5까지 {
    출력("i = " + 타입(i))
}

# 섹션 7: 함수
출력("\n6. 함수 테스트:")
더하기 = 함수(a, b) {
    반환 a + b
}
결과 = 더하기(10, 20)
출력("10 + 20 = " + 타입(결과))

# 섹션 8: 배열
출력("\n7. 배열 테스트:")
숫자들 = [1, 2, 3, 4, 5]
출력("배열: ")
출력(숫자들)
출력("길이: " + 타입(길이(숫자들)))

# 섹션 9: 조사 표현식
출력("\n8. 조사 표현식 테스트:")
가져오기 "stdlib/math"
데이터 = [5, 2, 8, 1, 9]
정렬된 = 데이터를 정렬한다
출력("정렬 결과: ")
출력(정렬된)

# 섹션 10: 클로저
출력("\n9. 클로저 테스트:")
만들기_카운터 = 함수() {
    개수 = 0
    반환 함수() {
        개수 = 개수 + 1
        반환 개수
    }
}
카운터 = 만들기_카운터()
출력("카운터 1: " + 타입(카운터()))
출력("카운터 2: " + 타입(카운터()))
출력("카운터 3: " + 타입(카운터()))

출력("\n=== 튜토리얼 예제 테스트 완료 ===")
