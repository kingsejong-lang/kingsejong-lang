# LSP 고급 기능 테스트 파일
# 이 파일로 다음 기능들을 테스트할 수 있습니다:
# 1. Go to Definition (F12) - 변수나 함수 이름 위에서 F12
# 2. Hover Information - 변수나 함수 위에 마우스 올리기
# 3. Find References (Shift+F12) - 사용처 찾기
# 4. Rename (F2) - 심볼 이름 변경

# 전역 변수
정수 x = 10
정수 y = 20
정수 합계 = x + y

# 함수 정의
함수 더하기(a, b) {
    # 로컬 변수
    정수 결과 = a + b
    반환 결과
}

# 함수 호출
정수 값1 = 더하기(5, 3)
정수 값2 = 더하기(10, 20)
정수 값3 = 더하기(x, y)

# 전역/로컬 스코프 테스트
정수 count = 0

함수 증가() {
    정수 count = 1  # 로컬 count
    정수 temp = count + 1
    반환 temp
}

정수 결과1 = 증가()
정수 결과2 = count + 1  # 전역 count 참조

# 재귀 함수
함수 피보나치(n) {
    만약 (n <= 1) {
        반환 n
    }
    반환 피보나치(n - 1) + 피보나치(n - 2)
}

정수 fib5 = 피보나치(5)
정수 fib10 = 피보나치(10)

# 테스트 방법:
# 1. "x" 위에 커서를 놓고 F12 -> 정의로 이동 (line 7)
# 2. "더하기" 위에 마우스 올리기 -> "함수 더하기(a, b)" 표시
# 3. "count" 위에서 Shift+F12 -> 모든 사용처 표시 (전역/로컬 구분)
# 4. "결과" 위에서 F2 -> 이름 변경 (result로 변경 시도)
