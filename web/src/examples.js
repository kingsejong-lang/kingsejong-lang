/**
 * KingSejong 예제 프로그램 모음
 */

export const examples = [
    {
        title: '안녕하세요, 세계!',
        description: '가장 기본적인 Hello World 프로그램',
        code: `출력("안녕하세요, KingSejong!")
출력("한글로 프로그래밍하는 자연스러운 언어")`
    },
    {
        title: '변수와 사칙연산',
        description: '변수를 선언하고 사칙연산을 수행합니다',
        code: `정수 a = 10
정수 b = 5

출력("=== 간단한 계산기 ===")
출력("a =", a)
출력("b =", b)
출력("")
출력("a + b =", a + b)
출력("a - b =", a - b)
출력("a * b =", a * b)
출력("a / b =", a / b)
출력("a % b =", a % b)`
    },
    {
        title: '조건문',
        description: '조건에 따라 다른 동작을 수행합니다',
        code: `정수 점수 = 85

출력("점수:", 점수)

만약 (점수 >= 90) {
    출력("등급: A")
} 아니면 만약 (점수 >= 80) {
    출력("등급: B")
} 아니면 만약 (점수 >= 70) {
    출력("등급: C")
} 아니면 {
    출력("등급: F")
}`
    },
    {
        title: '반복문',
        description: '반복문으로 1부터 10까지 출력합니다',
        code: `출력("=== 1부터 10까지 ===")

정수 i = 1
10번 반복한다 {
    출력(i)
    i = i + 1
}`
    },
    {
        title: '함수 정의',
        description: '함수를 정의하고 호출합니다',
        code: `정수 더하기 = 함수(a, b) {
    반환 a + b
}

정수 곱하기 = 함수(a, b) {
    반환 a * b
}

출력("3 + 5 =", 더하기(3, 5))
출력("3 * 5 =", 곱하기(3, 5))

정수 제곱 = 함수(n) {
    반환 곱하기(n, n)
}

출력("5의 제곱 =", 제곱(5))`
    },
    {
        title: '재귀 함수 - 팩토리얼',
        description: '재귀 함수로 팩토리얼을 계산합니다',
        code: `정수 팩토리얼 = 함수(n) {
    만약 (n <= 1) {
        반환 1
    }
    반환 n * 팩토리얼(n - 1)
}

출력("=== 팩토리얼 ===")
출력("5! =", 팩토리얼(5))
출력("10! =", 팩토리얼(10))`
    },
    {
        title: '재귀 함수 - 피보나치',
        description: '재귀 함수로 피보나치 수열을 계산합니다',
        code: `정수 피보나치 = 함수(n) {
    만약 (n <= 1) {
        반환 n
    }
    반환 피보나치(n-1) + 피보나치(n-2)
}

출력("=== 피보나치 수열 ===")
정수 i = 0
10번 반복한다 {
    출력("F(", i, ") =", 피보나치(i))
    i = i + 1
}`
    },
    {
        title: '배열 기본',
        description: '배열을 생성하고 요소에 접근합니다',
        code: `출력("=== 배열 기본 ===")

과일들 = ["사과", "바나나", "오렌지", "포도"]
출력("과일 목록:", 과일들)
출력("첫 번째 과일:", 과일들[0])
출력("두 번째 과일:", 과일들[1])

숫자들 = [1, 2, 3, 4, 5]
출력("\n숫자 목록:", 숫자들)
출력("배열 길이:", 길이(숫자들))`
    },
    {
        title: '배열 메서드',
        description: '배열의 다양한 메서드를 사용합니다',
        code: `출력("=== 배열 정렬 ===")
숫자들 = [3, 1, 4, 1, 5, 9, 2, 6]
출력("원본:", 숫자들)
정렬된배열 = 숫자들을 정렬한다
출력("정렬:", 정렬된배열)

출력("\n=== 배열 역순 ===")
원본 = [1, 2, 3, 4, 5]
출력("원본:", 원본)
역순 = 원본을 역순으로_나열한다
출력("역순:", 역순)`
    },
    {
        title: '클로저',
        description: '클로저를 사용한 카운터 예제',
        code: `출력("=== 클로저 (Closure) 예제 ===")

카운터생성 = 함수() {
    count = 0
    반환 함수() {
        count = count + 1
        반환 count
    }
}

counter1 = 카운터생성()
출력("카운터 1:")
출력(counter1())
출력(counter1())
출력(counter1())

counter2 = 카운터생성()
출력("\n카운터 2 (독립적):")
출력(counter2())
출력(counter2())`
    },
];
